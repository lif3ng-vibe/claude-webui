{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面壳主窗口权限：core 默认 + event（日志 listen）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}